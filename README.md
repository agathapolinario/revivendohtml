<h1> Revivendo o HTML </h1>

Repositório criado para revisar e aprimorar meus conhecimentos em **HTML5**.  
Este é o primeiro passo da minha jornada de volta ao desenvolvimento front-end, começando pelo básico: a estrutura de um documento HTML 

<h2>🧠 O que aprendi aqui </h2> 

- Como declarar o tipo de documento com `<!DOCTYPE html>`;
- A diferença entre as seções **head** e **body**;
- Como definir o idioma da página com `lang="pt-br"`;
- A importância das **metatags**, como `<meta charset="UTF-8">`;
- Como aplicar estilos simples dentro da tag `<style>`;
- Hierarquia de títulos com `<h1>` e `<h2>`;
- Pequenos toques de estilo, como **cor** e **sombra no texto**.

